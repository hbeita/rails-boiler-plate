<%= form_with(model: membership_church_service) do |form| %>
  <% if membership_church_service.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(membership_church_service.errors.count, "error") %> prohibited this membership_church_service from being saved:</h2>

      <ul>
        <% membership_church_service.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :date %>
    <%= form.date_select :date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
